export default interface IAtracoes {
  id: number;
  imagem: string;
  nome: string;
  local: string;
  avaliacao: number;
  comentario: number;
  preco: number;
  createdAt: string;
  updatedAt: string;
}
