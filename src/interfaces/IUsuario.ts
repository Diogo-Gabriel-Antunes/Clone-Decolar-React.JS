export default interface IUsuario {
  id: number;
  nome: string;
  email: string;
  role: string;
  foto: string;
  pontos: number;
  pontosPendentes: number;
  createdAt: string;
  updatedAt: string;
}
